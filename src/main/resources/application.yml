spring:
  datasource:
    url: jdbc:mysql://localhost:3306/traindb
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
//TODO GTB：思考一下，这里用create合适吗？
